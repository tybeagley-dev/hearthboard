{
  "name": "nestboard",
  "private": true,
  "workspaces": [
    "apps/dashboard"
  ],
  "scripts": {
    "dev": "concurrently --names api,web --prefix-colors cyan,magenta \"npm run dev --prefix api\" \"npm run dev --workspace apps/dashboard\"",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d db",
    "db:pull": "docker run --rm postgres:18 pg_dump \"$PROD_DATABASE_URL\" --data-only --no-owner --no-privileges | psql postgresql://nestboard:nestboard@localhost:5433/nestboard"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
